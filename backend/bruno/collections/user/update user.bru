meta {
  name: update user
  type: http
  seq: 4
}

put {
  url: {{host}}:{{port}}/{{prefix}}{{version}}/user/2
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "id": 2,
    "email": "sandy.companie@gmail.com"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
