meta {
  name: reactivate user
  type: http
  seq: 6
}

post {
  url: {{host}}:{{port}}/{{prefix}}{{version}}/user/reactivate/2
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "id": 2 
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
