meta {
  name: get all users
  type: http
  seq: 3
}

get {
  url: {{host}}:{{port}}/{{prefix}}{{version}}/user/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
