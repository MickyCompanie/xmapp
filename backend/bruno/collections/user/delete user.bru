meta {
  name: delete user
  type: http
  seq: 5
}

delete {
  url: {{host}}:{{port}}/{{prefix}}{{version}}/user/2
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "id": 2
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
