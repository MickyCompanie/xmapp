meta {
  name: get all wishes
  type: http
  seq: 1
}

get {
  url: {{host}}:{{port}}/{{prefix}}{{version}}/wish/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
